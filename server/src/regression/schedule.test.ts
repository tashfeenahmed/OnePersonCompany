/* The daily catch-up rule moved to shared/time.ts, where its three cases are
   reproduced beside the rest of the clock. This file is kept as the pointer,
   because a regression test that vanishes leaves no trace of where it went. */
export {};
